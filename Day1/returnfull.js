/*reate a function which return the full name of the person but you have to enter in a way 
of firstName and lastName and it returns in buy adding them*/
function fullName(a, b) {
    return `the name is ${a} ${b}`;
}

let name = fullName("shivmani", "singh");
console.log(name);
